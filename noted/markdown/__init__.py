"""The module provides fields for `notes` app models.

Fields have configuration in the project `settings.py` file.

**Fields**
    RenderedMarkdownField: renderedMarkdownField is just HTML
        code in plain/text format.
    MarkdownField: a markdown editor (with preview) for Django.

**Dependencies**
    django: a high-level Python web framework.
    markdown2: a fast and complete Python implementation of Markdown.
    django-simplemde: a markdown editor (with preview) for Django.

"""
